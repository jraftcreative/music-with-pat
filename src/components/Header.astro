---
interface Props {
  currentPage?: string
}

const { currentPage = '/' } = Astro.props

const navLinks = [
  { href: '/about/', label: 'About' },
  { href: '/testimonials/', label: 'Testimonials' },
  { href: '/blog/', label: 'Blog' },
  { href: '/faq/', label: 'FAQ' },
]

const lessonsDropdown = [
  { href: '/childrens-piano-lessons/', label: 'Piano Lessons for Children' },
  { href: '/childrens-violin-lessons/', label: 'Violin Lessons for Children' },
  { href: '/adult-piano-lessons/', label: 'Adult Piano Lessons' },
  { href: '/adult-violin-lessons/', label: 'Adult Violin Lessons' },
  { href: '/graded-piano-lessons/', label: 'Graded Piano Lessons' },
  { href: '/dsa-piano-lessons/', label: 'DSA Piano Lessons' },
  { href: '/music-theory-lessons-singapore/', label: 'Music Theory Lessons' },
]

const locationsDropdown = [
  { href: '/music-lessons-tengah/', label: 'Tengah' },
  { href: '/music-lessons-bukit-panjang/', label: 'Bukit Panjang' },
  { href: '/music-lessons-choa-chu-kang/', label: 'Choa Chu Kang' },
  { href: '/music-lessons-jurong-east/', label: 'Jurong East' },
  { href: '/music-lessons-jurong-west/', label: 'Jurong West' },
  { href: '/music-lessons-bukit-batok/', label: 'Bukit Batok' },
  { href: '/music-lessons-bukit-timah/', label: 'Bukit Timah' },
  { href: '/music-lessons-clementi/', label: 'Clementi' },
  { href: '/music-lessons-singapore/', label: 'View All Locations', divider: true },
]
---

<header class="py-4 md:py-6 px-6 md:px-12 relative sticky top-0 bg-primary-50/95 backdrop-blur-sm z-50 border-b border-primary-100">
  <div class="max-w-6xl mx-auto flex items-center justify-between">
    <a href="/" class="flex items-center gap-2">
      <img
        src="/music-with-pat-logo.webp"
        alt="Music with Pat - Music Lessons Singapore"
        class="h-10 md:h-12 w-auto"
        width="400"
        height="400"
      />
      <span class="text-base sm:text-lg md:text-xl font-semibold text-base-800">Music With Pat</span>
    </a>

    <!-- Desktop Navigation -->
    <nav class="hidden md:flex items-center gap-8">
      <a
        href="/about/"
        class={`transition-colors ${currentPage === '/about/' ? 'text-primary-600 font-medium' : 'text-base-600 hover:text-primary-600'}`}
      >
        About
      </a>

      <!-- Lessons Dropdown -->
      <div class="relative" id="lessons-dropdown">
        <button
          class={`flex items-center gap-1 transition-colors ${currentPage?.startsWith('/lessons') || lessonsDropdown.some(l => currentPage === l.href) ? 'text-primary-600 font-medium' : 'text-base-600 hover:text-primary-600'}`}
          id="lessons-btn"
          aria-expanded="false"
          aria-haspopup="true"
          aria-controls="lessons-panel"
        >
          Lessons
          <svg class="w-4 h-4 transition-transform" id="lessons-chevron" fill="none" viewBox="0 0 24 24" stroke-width="2" stroke="currentColor">
            <path stroke-linecap="round" stroke-linejoin="round" d="M19.5 8.25l-7.5 7.5-7.5-7.5" />
          </svg>
        </button>
        <div id="lessons-panel" class="hidden absolute top-full left-1/2 -translate-x-1/2 pt-2 w-56">
          <div class="bg-white rounded-xl shadow-lg border border-base-200 py-2">
            {lessonsDropdown.map(link => (
              <a
                href={link.href}
                class={`block px-4 py-2.5 text-sm transition-colors ${currentPage === link.href ? 'text-primary-600 bg-primary-50 font-medium' : 'text-base-600 hover:text-primary-600 hover:bg-primary-50'}`}
              >
                {link.label}
              </a>
            ))}
          </div>
        </div>
      </div>

      <!-- Locations Dropdown -->
      <div class="relative" id="locations-dropdown">
        <button
          class={`flex items-center gap-1 transition-colors ${locationsDropdown.some(l => currentPage === l.href) ? 'text-primary-600 font-medium' : 'text-base-600 hover:text-primary-600'}`}
          id="locations-btn"
          aria-expanded="false"
          aria-haspopup="true"
          aria-controls="locations-panel"
        >
          Locations
          <svg class="w-4 h-4 transition-transform" id="locations-chevron" fill="none" viewBox="0 0 24 24" stroke-width="2" stroke="currentColor">
            <path stroke-linecap="round" stroke-linejoin="round" d="M19.5 8.25l-7.5 7.5-7.5-7.5" />
          </svg>
        </button>
        <div id="locations-panel" class="hidden absolute top-full left-1/2 -translate-x-1/2 pt-2 w-52">
          <div class="bg-white rounded-xl shadow-lg border border-base-200 py-2">
            {locationsDropdown.map(link => (
              <a
                href={link.href}
                class={`block px-4 py-2.5 text-sm transition-colors ${link.divider ? 'border-t border-base-100 mt-1 pt-3 font-medium text-primary-600 hover:text-primary-700' : currentPage === link.href ? 'text-primary-600 bg-primary-50 font-medium' : 'text-base-600 hover:text-primary-600 hover:bg-primary-50'}`}
              >
                {link.label}
              </a>
            ))}
          </div>
        </div>
      </div>

      {navLinks.filter(link => link.label !== 'About').map(link => (
        <a
          href={link.href}
          class={`transition-colors ${currentPage === link.href ? 'text-primary-600 font-medium' : 'text-base-600 hover:text-primary-600'}`}
        >
          {link.label}
        </a>
      ))}
      <a href="/contact/" class={`px-5 py-2.5 rounded-full transition-colors ${currentPage === '/contact/' ? 'bg-primary-700 text-white' : 'bg-primary-600 text-white hover:bg-primary-700'}`}>
        Book a Trial
      </a>
    </nav>

    <!-- Mobile: Hamburger menu -->
    <div class="md:hidden">
      <button
        id="mobile-menu-btn"
        class="p-2 text-base-600 hover:text-primary-600 hover:bg-primary-50 rounded-lg transition-colors"
        aria-label="Open menu"
      >
        <svg id="menu-icon" class="w-6 h-6" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor">
          <path stroke-linecap="round" stroke-linejoin="round" d="M3.75 6.75h16.5M3.75 12h16.5m-16.5 5.25h16.5" />
        </svg>
        <svg id="close-icon" class="w-6 h-6 hidden" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor">
          <path stroke-linecap="round" stroke-linejoin="round" d="M6 18L18 6M6 6l12 12" />
        </svg>
      </button>
    </div>
  </div>

  <!-- Mobile dropdown menu -->
  <div id="mobile-menu" class="hidden md:hidden absolute top-full left-0 right-0 bg-white border-t border-base-200 shadow-lg z-50">
    <nav class="flex flex-col p-4 gap-2">
      <a
        href="/about/"
        class={`mobile-nav-link px-4 py-3 rounded-xl transition-colors ${currentPage === '/about/' ? 'text-primary-600 bg-primary-50 font-medium' : 'text-base-600 hover:text-primary-600 hover:bg-primary-50'}`}
      >
        About
      </a>

      <!-- Mobile Lessons Accordion -->
      <div>
        <button
          id="mobile-lessons-btn"
          class={`w-full flex items-center justify-between px-4 py-3 rounded-xl transition-colors ${lessonsDropdown.some(l => currentPage === l.href) ? 'text-primary-600 bg-primary-50 font-medium' : 'text-base-600 hover:text-primary-600 hover:bg-primary-50'}`}
          aria-expanded="false"
          aria-haspopup="true"
          aria-controls="mobile-lessons-panel"
        >
          Lessons
          <svg class="w-4 h-4 transition-transform" id="mobile-lessons-chevron" fill="none" viewBox="0 0 24 24" stroke-width="2" stroke="currentColor">
            <path stroke-linecap="round" stroke-linejoin="round" d="M19.5 8.25l-7.5 7.5-7.5-7.5" />
          </svg>
        </button>
        <div id="mobile-lessons-panel" class="flex flex-col gap-1 pl-4 mt-1" style="display:none">
          {lessonsDropdown.map(link => (
            <a
              href={link.href}
              class={`mobile-nav-link px-4 py-2.5 rounded-xl text-sm transition-colors ${currentPage === link.href ? 'text-primary-600 bg-primary-50 font-medium' : 'text-base-600 hover:text-primary-600 hover:bg-primary-50'}`}
            >
              {link.label}
            </a>
          ))}
        </div>
      </div>

      <!-- Mobile Locations Accordion -->
      <div>
        <button
          id="mobile-locations-btn"
          class={`w-full flex items-center justify-between px-4 py-3 rounded-xl transition-colors ${locationsDropdown.some(l => currentPage === l.href) ? 'text-primary-600 bg-primary-50 font-medium' : 'text-base-600 hover:text-primary-600 hover:bg-primary-50'}`}
          aria-expanded="false"
          aria-haspopup="true"
          aria-controls="mobile-locations-panel"
        >
          Locations
          <svg class="w-4 h-4 transition-transform" id="mobile-locations-chevron" fill="none" viewBox="0 0 24 24" stroke-width="2" stroke="currentColor">
            <path stroke-linecap="round" stroke-linejoin="round" d="M19.5 8.25l-7.5 7.5-7.5-7.5" />
          </svg>
        </button>
        <div id="mobile-locations-panel" class="flex flex-col gap-1 pl-4 mt-1" style="display:none">
          {locationsDropdown.map(link => (
            <a
              href={link.href}
              class={`mobile-nav-link px-4 py-2.5 rounded-xl text-sm transition-colors ${link.divider ? 'font-medium text-primary-600' : currentPage === link.href ? 'text-primary-600 bg-primary-50 font-medium' : 'text-base-600 hover:text-primary-600 hover:bg-primary-50'}`}
            >
              {link.label}
            </a>
          ))}
        </div>
      </div>

      {navLinks.filter(link => link.label !== 'About').map(link => (
        <a
          href={link.href}
          class={`mobile-nav-link px-4 py-3 rounded-xl transition-colors ${currentPage === link.href ? 'text-primary-600 bg-primary-50 font-medium' : 'text-base-600 hover:text-primary-600 hover:bg-primary-50'}`}
        >
          {link.label}
        </a>
      ))}
      <a href="/contact/" class="mobile-nav-link px-4 py-3 bg-primary-600 text-white text-center rounded-xl hover:bg-primary-700 transition-colors font-medium">
        Book a Trial
      </a>
    </nav>
  </div>
</header>

<script>
  const mobileMenuBtn = document.getElementById('mobile-menu-btn');
  const mobileMenu = document.getElementById('mobile-menu');
  const menuIcon = document.getElementById('menu-icon');
  const closeIcon = document.getElementById('close-icon');

  function toggleMobileMenu() {
    mobileMenu?.classList.toggle('hidden');
    menuIcon?.classList.toggle('hidden');
    closeIcon?.classList.toggle('hidden');
  }

  mobileMenuBtn?.addEventListener('click', toggleMobileMenu);

  // Desktop lessons dropdown
  const lessonsDropdown = document.getElementById('lessons-dropdown');
  const lessonsPanel = document.getElementById('lessons-panel');
  const lessonsChevron = document.getElementById('lessons-chevron');

  lessonsDropdown?.addEventListener('mouseenter', () => {
    lessonsPanel?.classList.remove('hidden');
    lessonsChevron?.classList.add('rotate-180');
  });

  lessonsDropdown?.addEventListener('mouseleave', () => {
    lessonsPanel?.classList.add('hidden');
    lessonsChevron?.classList.remove('rotate-180');
  });

  // Also toggle on click for touch devices
  const lessonsBtn = document.getElementById('lessons-btn');
  lessonsBtn?.addEventListener('click', () => {
    const isOpen = lessonsPanel?.classList.toggle('hidden');
    lessonsChevron?.classList.toggle('rotate-180');
    lessonsBtn.setAttribute('aria-expanded', lessonsPanel?.classList.contains('hidden') ? 'false' : 'true');
  });

  // Mobile lessons accordion
  const mobileLessonsBtn = document.getElementById('mobile-lessons-btn');
  const mobileLessonsPanel = document.getElementById('mobile-lessons-panel');
  const mobileLessonsChevron = document.getElementById('mobile-lessons-chevron');

  mobileLessonsBtn?.addEventListener('click', () => {
    if (mobileLessonsPanel) {
      const isHidden = mobileLessonsPanel.style.display === 'none';
      mobileLessonsPanel.style.display = isHidden ? 'flex' : 'none';
      mobileLessonsBtn.setAttribute('aria-expanded', isHidden ? 'true' : 'false');
    }
    mobileLessonsChevron?.classList.toggle('rotate-180');
  });

  // Desktop locations dropdown
  const locationsDropdownEl = document.getElementById('locations-dropdown');
  const locationsPanel = document.getElementById('locations-panel');
  const locationsChevron = document.getElementById('locations-chevron');

  locationsDropdownEl?.addEventListener('mouseenter', () => {
    locationsPanel?.classList.remove('hidden');
    locationsChevron?.classList.add('rotate-180');
  });

  locationsDropdownEl?.addEventListener('mouseleave', () => {
    locationsPanel?.classList.add('hidden');
    locationsChevron?.classList.remove('rotate-180');
  });

  const locationsBtn = document.getElementById('locations-btn');
  locationsBtn?.addEventListener('click', () => {
    locationsPanel?.classList.toggle('hidden');
    locationsChevron?.classList.toggle('rotate-180');
    locationsBtn.setAttribute('aria-expanded', locationsPanel?.classList.contains('hidden') ? 'false' : 'true');
  });

  // Mobile locations accordion
  const mobileLocationsBtn = document.getElementById('mobile-locations-btn');
  const mobileLocationsPanel = document.getElementById('mobile-locations-panel');
  const mobileLocationsChevron = document.getElementById('mobile-locations-chevron');

  mobileLocationsBtn?.addEventListener('click', () => {
    if (mobileLocationsPanel) {
      const isHidden = mobileLocationsPanel.style.display === 'none';
      mobileLocationsPanel.style.display = isHidden ? 'flex' : 'none';
      mobileLocationsBtn.setAttribute('aria-expanded', isHidden ? 'true' : 'false');
    }
    mobileLocationsChevron?.classList.toggle('rotate-180');
  });

  // Close menus on outside click
  document.addEventListener('click', (e) => {
    if (!mobileMenuBtn?.contains(e.target as Node) && !mobileMenu?.contains(e.target as Node)) {
      mobileMenu?.classList.add('hidden');
      menuIcon?.classList.remove('hidden');
      closeIcon?.classList.add('hidden');
    }
    if (!lessonsDropdown?.contains(e.target as Node)) {
      lessonsPanel?.classList.add('hidden');
      lessonsChevron?.classList.remove('rotate-180');
    }
    if (!locationsDropdownEl?.contains(e.target as Node)) {
      locationsPanel?.classList.add('hidden');
      locationsChevron?.classList.remove('rotate-180');
    }
  });
</script>
